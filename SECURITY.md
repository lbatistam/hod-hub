# Segurança

Não publique vulnerabilidades em issues abertas. Use o canal privado do mantenedor do repositório.

Nunca anexe `.env`, tokens OAuth, cookies, bancos SQLite, logs, backups ou dados reais de calendário. Se um segredo for exposto, revogue-o imediatamente e remova-o do histórico Git.

